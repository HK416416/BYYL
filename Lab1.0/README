Lab1 — 词法与语法分析器（C-- 子集）

简介
----
本目录实现了一个基于 Flex/Bison 的简化 C-like 语言前端（词法分析、语法分析、AST 输出与错误报告），用于课程实验 1.0。主要代码位于 `Code/`，测试用例位于 `Test/`，实验报告在 `report.md`。

目录结构
----
- Code/: 源代码和 Makefile
	- `lexical.l` — Flex 词法文件
	- `syntax.y` — Bison 语法与语义动作
	- `main.c` — AST 实现与程序入口
	- `Makefile` — 本地构建脚本（直接调用 flex/bison/gcc）
- Test/: 示例输入文件（.cmm）
- report.md: 实验报告（实现方法、遇到的问题与测试说明）

快速使用
----
在 `Lab1.0/Code` 下执行：

```bash
cd Lab1.0/Code
make        # 生成 lex.yy.c, syntax.tab.c 并编译为可执行 parser
./parser ../Test/m4_test.cmm   # 运行示例
```

输出说明
----
- 正常情况下，程序在 stdout 打印 AST（前序遍历，两个空格缩进）。
- 词法/语法错误按评分要求输出到 stdout，格式示例：
	- `Error type A at Line N: ...`（词法错误）
	- `Error type B at Line N: ...`（语法错误）
- 若发生任意错误，程序不会打印 AST（由全局 `has_error` 控制）.




