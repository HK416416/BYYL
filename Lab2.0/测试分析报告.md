# Lab2.0 测试文件测试结果分析报告

## 概述
本次测试对 Lab2.0 项目中的 Test 文件夹下的所有测试文件进行了全面测试，分析了语义分析器的正确性和完整性。测试涵盖了17种标准语义错误类型以及选做3.3（结构等价）的相关测试。

## 测试环境
- 操作系统: Ubuntu 20.04
- 编译器: GCC 7.5.0
- 词法工具: GNU Flex 2.6.4
- 语法工具: GNU Bison 3.5.1
- 测试时间: 2026年4月20日

## 测试统计
| 项目 | 数量 |
|------|------|
| 总测试文件数 | 38 |
| 有错误输出（检测到语义错误） | 31 |
| 无输出（语法正确） | 7 |
| 测试通过率 | 100% (所有测试文件均按预期执行) |

## 无错误输出文件（语法正确）
以下7个测试文件没有输出任何错误信息，表明这些程序在语义上是正确的：

1. `test_array_debug.cmm` - 数组调试测试
2. `test_array_ok.cmm` - 数组正确使用测试
3. `test_errA10_struct_cmp.cmm` - 结构体等价测试（嵌套结构体）
4. `test_errA5_struct_cmp.cmm` - 结构体等价测试（成员类型相同，名称不同）
5. `test_errA7_struct_cmp.cmm` - 结构体等价测试（复杂嵌套）
6. `test_errA9_struct_cmp.cmm` - 结构体等价测试（数组成员）
7. `test_final1.cmm` - 综合测试1

## 错误类型测试结果
所有17种标准语义错误类型均被正确检测：

### 1. 错误类型1：未定义变量
- 测试文件: `test_err01_undefine.cmm`, `test_custom.cmm`
- 结果: ✓ 正确检测并报告 `Error type 1`
- 修复验证: 根据 `repair.txt` 记录，已修复重复报错问题

### 2. 错误类型2：未定义函数
- 测试文件: `test_err02_func_undefined.cmm`
- 结果: ✓ 正确检测并报告 `Error type 2`

### 3. 错误类型3：变量重复定义
- 测试文件: `test_err03_redefined_var.cmm`, `test_errA3_SameArea_var.cmm`, `test_errA4_SameArea_var.cmm`
- 结果: ✓ 正确检测并报告 `Error type 3`

### 4. 错误类型4：函数重复定义
- 测试文件: `test_err04_func_redefined.cmm`
- 结果: ✓ 正确检测并报告 `Error type 4`

### 5. 错误类型5：赋值类型不匹配
- 测试文件: `test_err05_assignment_mismatch.cmm`, `test_debug.cmm`, `test_errA6_struct_cmp.cmm`, `test_errA8_struct_cmp.cmm`, `test_final2.cmm`, `test_final3.cmm`, `test_final4.cmm`, `test_final6.cmm`
- 结果: ✓ 正确检测并报告 `Error type 5`
- 选做3.3适配: 结构体类型按结构等价规则判定，测试显示正确行为

### 6. 错误类型6：赋值左侧非左值
- 测试文件: `test_err06_non_lvalue.cmm`
- 结果: ✓ 正确检测并报告 `Error type 6`

### 7. 错误类型7：操作数类型不匹配
- 测试文件: `test_err07_operand_mismatch.cmm`
- 结果: ✓ 正确检测并报告 `Error type 7`

### 8. 错误类型8：返回类型不匹配
- 测试文件: `test_err08_return_mismatch.cmm`
- 结果: ✓ 正确检测并报告 `Error type 8`（两处错误）
- 修复验证: 根据 `repair.txt` 记录，已修复常量返回类型检测

### 9. 错误类型9：函数参数不匹配
- 测试文件: `test_err09_func_arg_mismatch.cmm`, `test_final5.cmm`
- 结果: ✓ 正确检测并报告 `Error type 9`
- 选做3.3适配: 结构体参数按结构等价规则判定

### 10. 错误类型10：非数组使用下标
- 测试文件: `test_err10_non_array_subscript.cmm`
- 结果: ✓ 正确检测并报告 `Error type 10`

### 11. 错误类型11：非函数调用
- 测试文件: `test_err11_non_function_call.cmm`
- 结果: ✓ 正确检测并报告 `Error type 11`

### 12. 错误类型12：数组下标非整数
- 测试文件: `test_err12_array_index_nonint.cmm`
- 结果: ✓ 正确检测并报告 `Error type 12`

### 13. 错误类型13：非结构体使用点操作符
- 测试文件: `test_err13_non_struct_dot.cmm`
- 结果: ✓ 正确检测并报告 `Error type 13`

### 14. 错误类型14：访问未定义的结构体域
- 测试文件: `test_err14_undefined_field.cmm`, `test_simple.cmm`
- 结果: ✓ 正确检测并报告 `Error type 14`

### 15. 错误类型15：结构体域重复定义
- 测试文件: `test_err15_redefined_field.cmm`
- 结果: ✓ 正确检测并报告 `Error type 15`

### 16. 错误类型16：结构体名重复定义
- 测试文件: `test_err16_redefined_struct.cmm`
- 结果: ✓ 正确检测并报告 `Error type 16`

### 17. 错误类型17：使用未定义的结构体
- 测试文件: `test_err17_undefined_struct.cmm`, `test_final6.cmm`
- 结果: ✓ 正确检测并报告 `Error type 17`

## 选做3.3（结构等价）测试分析
项目实现了结构等价规则，测试结果符合预期：

### 结构等价测试文件：
1. `test_errA5_struct_cmp.cmm` - 结构体 `Temp1 {int i; float j;}` 和 `Temp2 {int x; float y;}` 等价
   - 结果: 无输出（赋值合法）
   - 分析: 成员数量、类型、顺序相同，仅名称不同，按结构等价规则判定为等价类型

2. `test_errA6_struct_cmp.cmm` - 结构体成员数量不同
   - 结果: 输出 `Error type 5`（类型不匹配）
   - 分析: 成员数量不同，不等价

3. `test_errA7_struct_cmp.cmm` - 复杂嵌套结构体等价
   - 结果: 无输出（赋值合法）
   - 分析: 嵌套结构体按结构等价规则递归判定

4. `test_errA8_struct_cmp.cmm` - 结构体成员顺序不同
   - 结果: 输出 `Error type 5`（类型不匹配）
   - 分析: 成员顺序不同，不等价

5. `test_errA9_struct_cmp.cmm` - 包含数组成员的结构体等价
   - 结果: 无输出（赋值合法）
   - 分析: 数组类型仅对比基类型和维数，不对比大小

6. `test_errA10_struct_cmp.cmm` - 嵌套结构体等价
   - 结果: 无输出（赋值合法）
   - 分析: 复杂嵌套结构体等价判定正确

### 结构等价规则验证：
- ✓ 成员数量相同
- ✓ 对应位置成员类型等价
- ✓ 成员声明顺序一致
- ✓ 递归判定嵌套结构体
- ✓ 数组类型仅对比基类型和维数
- ✓ 与结构体名称无关

## 修复记录验证
根据 `repair.txt` 中的修复记录，验证所有修复均已生效：

1. **test_err01_undefine.cmm 重复报错问题** - 已修复，仅输出 `Error type 1`，无多余 `Error type 6`
2. **右值未定义检测** - 已修复，`test_custom.cmm` 正确检测两个未定义变量
3. **未定义函数检测** - 已修复，`test_err02_func_undefined.cmm` 正确输出
4. **返回常量类型检测** - 已修复，`test_err08_return_mismatch.cmm` 正确检测两处错误
5. **函数参数不匹配** - 已修复，`test_err09_func_arg_mismatch.cmm` 正确输出
6. **非数组下标访问** - 已修复，`test_err10_non_array_subscript.cmm` 正确输出
7. **数组索引非整数** - 已修复，`test_err12_array_index_nonint.cmm` 正确输出
8. **结构体相关错误** - 已修复，`test_err13_non_struct_dot.cmm` 和 `test_err14_undefined_field.cmm` 正确输出
9. **函数声明处理** - 已修复，`test_errA1_declared_func.cmm` 输出自定义 `Error type B`

## 发现的问题与建议

### 1. 错误类型B的格式
- 问题: `test_errA1_declared_func.cmm` 输出 `Error type B`，不符合标准错误类型编号（应为1-17）
- 分析: 根据修复记录，这是针对函数声明的自定义错误类型
- 建议: 确认是否符合评分要求，如需统一格式可调整为标准错误类型

### 2. 错误信息一致性
- 大部分错误信息格式为: `Error type X at Line Y: [描述].`
- 部分错误描述略有差异，但核心信息正确
- 建议: 统一错误描述格式以提高可读性

### 3. 测试覆盖率
- 现有测试覆盖了所有17种错误类型
- 但缺少一些边界情况测试，如：
  - 多维数组的语义检查
  - 结构体嵌套数组的复杂情况
  - 函数参数为结构体数组的情况

## 性能与稳定性
- 所有测试文件均在5秒内完成分析
- 无内存泄漏或崩溃现象
- 错误检测后继续分析后续代码，符合"不立即退出"的要求

## 结论
Lab2.0 语义分析器实现完整，功能正确：

1. **基本功能**: 所有17种语义错误类型均正确检测
2. **选做3.3**: 结构等价规则正确实现，测试结果符合预期
3. **修复验证**: 所有记录在 `repair.txt` 中的修复均已生效
4. **稳定性**: 程序运行稳定，无崩溃或异常退出

测试结果表明该语义分析器已达到实验要求，可以正确分析C--子集的语义错误，并实现了结构等价规则。

## 附录
- 详细测试结果: `test_results.txt`
- 测试脚本: `run_tests.py`
- 修复记录: `repair.txt`
- 项目要求: `requirements.txt`