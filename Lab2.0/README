Lab1 — 词法与语法分析器（C-- 子集）

简介
----
本目录实现了一个基于 Flex/Bison 的简化 C-like 语言前端（词法分析、语法分析、AST 输出与错误报告），用于课程实验 1.0。主要代码位于 `Code/`，测试用例位于 `Test/`，实验报告在 `report.md`。

目录结构
----
- Code/: 源代码和 Makefile
	- `lexical.l` — Flex 词法文件
	- `syntax.y` — Bison 语法与语义动作
	- `main.c` — AST 实现与程序入口
	- `Makefile` — 本地构建脚本（直接调用 flex/bison/gcc）
- Test/: 示例输入文件（.cmm）
- report.pdf: 实验报告（实现方法、遇到的问题与测试说明）

快速使用
----
在 `../Code` 下执行：

```bash
cd ../Code
make        # 生成 lex.yy.c, syntax.tab.c 并编译为可执行 parser
./parser <input.cmm>   # 运行示例
```

输出说明
----
- 正常情况下，程序在没有输出。
- 错误按评分要求输出到 stdout，格式示例：
	- `Error type x at Line N: ...`




