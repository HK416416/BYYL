Lab2 — 语义分析器（C-- 子集）

简介
----
本目录实现了一个基于AST遍历的C--语言语义分析器，在实验一得到的抽象语法树基础上，完成符号表构建、类型检查与17类语义错误的检测与报告。支持结构体类型的结构等价判定（选做3.3），并在发现错误后继续扫描以检出全部错误，满足自动化评测系统的精确比对要求。

目录结构
----
- Code/: 源代码和 Makefile
	- `lexical.l` — Flex 词法文件
	- `syntax.y` — Bison 语法与语义动作
	- `main.c` — AST 实现与程序入口
	- `semantic.h/c` — 语义分析接口与主逻辑
	- `symbol.h/c` — 符号表实现
	- `type.h/c` — 类型系统与结构等价判定
	- `Makefile` — 本地构建脚本（直接调用 flex/bison/gcc）

快速使用
----
在 `Code` 下执行：

```bash
cd Code
make        # 生成 lex.yy.c, syntax.tab.c 并编译为可执行 parser
./parser <input.cmm>   # 运行示例
```


输出说明
----
- 正常情况下（无语义错误），程序没有输出。
- 存在语义错误时，按以下格式输出到 stdout：
	- `Error type [错误类型编号] at Line [出错行号]: [错误说明文字].`

支持的语义错误类型（共17类）：
1. 变量在使用时未经定义
2. 函数在调用时未经定义
3. 变量重复定义，或变量与已定义结构体名重复
4. 函数重复定义（同名函数多次定义）
5. 赋值号两边的表达式类型不匹配（适配结构等价规则）
6. 赋值号左边出现只有右值的表达式
7. 操作数类型不匹配/操作数类型与操作符不匹配
8. return语句的返回类型与函数定义的返回类型不匹配
9. 函数调用时实参与形参的数目或类型不匹配（适配结构等价规则）
10. 对非数组型变量使用`[]`数组访问操作符
11. 对普通变量使用`()`函数调用操作符
12. 数组访问操作符`[]`中出现非整数
13. 对非结构体型变量使用`.`操作符
14. 访问结构体中未定义的域
15. 同一结构体中域名重复定义，或结构体域定义时进行初始化
16. 结构体名与已定义的结构体/变量名重复
17. 使用未定义的结构体定义变量

选做功能
----
- **选做3.3：结构体类型的结构等价判定**
  - 将结构体间的类型等价机制由名等价改为结构等价
  - 判定标准：成员数量完全相同、对应位置的成员类型完全等价、成员声明顺序完全一致，与结构体名称无关
  - 数组例外规则：数组类型仅对比基类型和维数，不对比数组大小
  - 递归判定：嵌套结构体按结构等价规则递归校验


实现
----
1. **完整的类型系统**：支持基本类型（int/float）、数组类型、结构体类型
2. **高效的符号表**：全局作用域哈希表（大小16384），PJW哈希函数
3. **错误恢复机制**：发现错误后继续遍历AST，输出所有错误而非遇错即停
4. **精确的错误定位**：递归向下遍历AST定位到ID终结符节点，获取准确行号
5. **结构等价递归判定**：支持嵌套结构体的递归等价比较






