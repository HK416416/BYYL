Lab3 — 中间代码生成器（C-- 子集）

简介
----
本目录实现了一个基于 AST 遍历的 C-- 语言中间代码翻译器，在前两个实验（词法/语法分析、语义分析）的基础上，对已有的抽象语法树进行语法制导翻译，输出符合教材表 4.6 规范的三地址码形式中间代码。必做部分覆盖基础数据类型、算术/关系/逻辑表达式、赋值、一维数组、控制流、函数定义与调用；选做 4.2 放宽限制，额外支持高维数组声明/访问以及一维数组作为函数参数（传地址）。

目录结构
----
- Code/: 源代码和 Makefile
	- `lexical.l` — Flex 词法文件
	- `syntax.y` — Bison 语法文件（RELOP 改为独立 LT/LE/GT/GE/EQ/NE 标记）
	- `main.c` — AST 实现、语义分析入口与中间代码翻译入口
	- `semantic.h/c` — 语义分析接口与主逻辑（含 `semantic_analysis_with_context`）
	- `symbol.h/c` — 符号表实现
	- `type.h/c` — 类型系统与结构等价判定
	- `translate.h` — 中间代码翻译接口
	- `translate.c` — 中间代码生成核心实现
	- `Makefile` — 本地构建脚本（直接调用 flex/bison/gcc）

快速使用
----
在 `Code` 下执行：

```bash
cd Code
make                                    # 编译生成 parser
./parser input.cmm output.ir            # 翻译单个源文件            
```

输出说明
----
- 正常情况下，输出文件（`.ir`）每行一条中间代码指令，格式遵循教材表 4.6。
- 遇到不支持的结构体类型时，**仅向 stdout** 输出错误信息，不生成 .ir 文件：
  ```
  Cannot translate: Code contains struct type variables or struct parameters.
  ```
- 遇到高维数组作为函数参数（违反选做 4.2 限制）时，同样仅向 stdout 输出错误信息，不生成 .ir 文件。

### 中间代码指令规范（表 4.6 关键指令）

| 语法                       | 说明                                 |
|---------------------------|--------------------------------------|
| `LABEL x :`               | 定义标号 `x`                          |
| `FUNCTION f :`            | 定义函数 `f`                          |
| `x := y`                  | 赋值                                 |
| `x := y + z`              | 加法（同样支持 `-` `*` `/`）          |
| `x := &y`                 | 取地址                               |
| `x := *y`                 | 读内存                               |
| `*x := y`                 | 写内存                               |
| `GOTO x`                  | 无条件跳转                           |
| `IF x relop y GOTO z`     | 条件跳转（relop: `<` `<=` `>` `>=` `==` `!=`） |
| `RETURN x`                | 返回 `x`（`x` 可为变量或 `#`常数）    |
| `DEC x size`              | 分配 `size` 字节（4 的倍数）          |
| `ARG x`                   | 传递实参                             |
| `x := CALL f`             | 调用函数 `f`，返回值存入 `x`          |
| `PARAM x`                 | 声明形参                             |
| `READ x`                  | 读入整数到 `x`                        |
| `WRITE x`                 | 输出整数 `x`                         |

> 注意：关键字必须大写；常数前必须加 `#`；变量名区分大小写；ARG 顺序与 PARAM 顺序相反；LABEL/FUNCTION 后名称与冒号之间空一格。

### 变量命名规则

- 临时变量：`t1, t2, t3, ...`（全局唯一，跨函数递增）
- 用户变量：`v1, v2, v3, ...`（全局唯一，跨函数递增）
- 标号：`label1, label2, label3, ...`（全局唯一，跨函数递增）

选做功能
----
- **选做 4.2：一维数组作为函数参数 + 高维数组**
  - 形参声明 `int arr[]` → `PARAM vN`，调用时 `ARG vN`（直接传基地址）
  - 高维数组（二维及以上）：正确计算 `DEC` 总空间，多维下标地址计算使用 `base + i1*stride1 + i2*stride2 + ...`
  - 高维数组不能作为函数参数（违反时输出错误提示，不生成 .ir）
  - 不支持结构体（选做 4.1），遇到结构体输出错误提示

测试用例
----
`Test/` 目录包含以下测试用例：

| 文件 | 类型 | 测试特性 |
|------|------|---------|
| `test_must1_sgn.cmm` | 必做 | if-else 嵌套、布尔表达式、read/write |
| `test_must2_fact.cmm` | 必做 | 函数定义、参数传递、递归调用 |
| `test_opt1_struct.cmm` | 选做 4.1 | 结构体检测（期望错误输出） |
| `test_opt2_array.cmm` | 选做 4.2 | 高维数组、一维数组作为函数参数 |
| `new_test1.cmm` | 自定义 | AND/OR/NOT 短路求值、多参数函数 |
| `new_test2.cmm` | 自定义 | 算术优先级、多参数函数、数组参数读写 |
| `new_test3.cmm` | 自定义 | while 循环、数组遍历、嵌套函数调用 |

实现
----
1. **语法制导翻译**：各语法结构对应独立的 `trans_*` 函数，递归遍历 AST 生成中间代码
2. **符号表集成**：复用实验二的符号表与类型系统，读/写函数预定义到符号表
3. **地址语义正确**：DEC 分配的数组变量名直接代表基地址，数组参数传入的也是基地址值
4. **全局唯一命名**：临时变量（t）、用户变量（v）、标号（label）均全局唯一，跨函数不重名
5. **错误输出到 stdout**：不支持的结构体/高维数组参数仅在 stdout 输出，不生成 .ir 文件
